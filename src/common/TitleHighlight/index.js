import styled from 'styled-components';
import highlight from 'styles/highlight';

const TitleHighlight = styled.div`
  margin-right: 16px;

  ${highlight}
`;

TitleHighlight.displayName = 'TitleHighlight';

export default TitleHighlight;
